name: Release

on:
  workflow_dispatch:    # 确保这行存在
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Test Workflow
        run: |
          echo "Workflow is running successfully!"
          echo "Tag: ${{ github.ref }}"
